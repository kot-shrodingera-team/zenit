// import { getElement, log } from '@kot-shrodingera-team/germes-utils';
// import {
//   balanceReady,
//   getNewValue,
//   updateBalance,
// } from '../stake_info/getBalance';

const afterSuccesfulLogin = async (): Promise<void> => {
  // const mainBlock = await getElement('.main-columns', 6666);
  // if (mainBlock) {
  //   const newValBalance = await getNewValue('.header-account-menu__balance');
  //   if (newValBalance) {
  //     log('Обновили баланс', 'orange');
  //     await balanceReady();
  //     updateBalance();
  //   }
  // }
};

export default afterSuccesfulLogin;
